# Senha do painel /admin. Fica só no servidor, nunca vai pro navegador.
ADMIN_PASSWORD=troque_por_uma_senha_forte

# Conexão do Postgres (Neon, Vercel Postgres, etc.)
DATABASE_URL=postgres://usuario:senha@host/banco?sslmode=require
